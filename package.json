{
  "name": "kindora-contract",
  "version": "0.0.0",
  "scripts": {
    "test": "npx hardhat test",
    "lint:solidity": "npx solhint 'contracts/**/*.sol' '**/test/**/*.sol' || true",
    "compile": "npx hardhat compile"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "2.2.3",
    "hardhat": "^2.17.0",
    "solhint": "^3.3.6",
    "crytic-slither": "^0.1.0"
  },
  "dependencies": {
  }
}
